# Web Framework
fastapi==0.115.14
uvicorn[standard]==0.35.0
pydantic==2.11.7
pydantic-settings==2.10.1

# Database
sqlalchemy==2.0.41
psycopg[binary]==3.3.4
alembic==1.12.1
pgvector==0.2.4

# AI/LangChain
# langgraph-prebuilt is pinned separately: newer prebuilt releases (>=1.0.1) import
# ExecutionInfo/ServerInfo from langgraph.runtime, which isn't defined until a
# later langgraph core release than the one langchain==1.0.0 allows (<1.1.0).
langgraph==1.0.10
langgraph-prebuilt==1.0.0
langchain==1.0.0
langchain-anthropic==0.3.15
voyageai==0.3.2

# Local (free) embeddings fallback: nomic-embed-text-v1.5
sentence-transformers==3.3.1
einops==0.8.0

# EasyWorship slide-change verification (OCR on the camera-2 feed)
easyocr==1.7.2

# Vision
opencv-python==4.10.0.84
numpy==2.2.0

# Audio (local mic/line-in capture for real-PCM voice diarization)
sounddevice==0.4.6

# HTTP
httpx==0.25.2
requests==2.31.0
websockets==12.0

# EasyWorship remote-protocol mDNS discovery (_ezwremote._tcp)
zeroconf==0.132.2

# Utilities
python-dotenv==1.0.0
structlog==23.2.0
python-multipart==0.0.6
onvif_zeep==0.7.8

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Development
black==23.12.1
pylint==3.0.3
mypy==1.7.1
isort==5.13.2
pre-commit==3.5.0
