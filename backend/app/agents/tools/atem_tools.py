"""AI tools for production control."""

# TODO: Implement tools for:
# - get_atem_state()
# - switch_camera(camera_id)
# - preview_camera(camera_id)
# - cut_to_preview()
# - auto_transition()
# - get_camera_state(camera_id)
# - move_camera_to_preset(camera_id, preset_name)
# - get_production_state()

# Each tool must:
# 1. Be wrapped as a LangChain tool
# 2. Have clear documentation
# 3. Validate inputs
# 4. Call backend services (not hardware directly)
# 5. Handle errors gracefully
# 6. Return structured results
